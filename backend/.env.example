# PostgreSQL connection string (also used by Alembic: alembic upgrade head)
# Local dev: localhost. On Railway: set DATABASE_URL from the Postgres plugin (Variables → Reference) — NOT localhost.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/review_insight

# Review provider: "mock", "offline", or "outscraper"
# mock     — random generic reviews, no API key needed
# offline  — curated realistic reviews from backend/data/offline/ (run: make seed-offline)
# outscraper — live Google Maps reviews (requires OUTSCRAPER_API_KEY)
REVIEW_PROVIDER=mock

# Outscraper API key — required when REVIEW_PROVIDER=outscraper
# Get your key at https://outscraper.com
OUTSCRAPER_API_KEY=

# Max reviews to fetch per Outscraper request (default: 100)
OUTSCRAPER_REVIEWS_LIMIT=100

# Outscraper review ordering (for real-review testing)
# sort: most_relevant | newest | highest_rating | lowest_rating
OUTSCRAPER_SORT=newest
# cutoff: optional Unix timestamp — only reviews newer than this (leave empty = all)
OUTSCRAPER_CUTOFF=
# OUTSCRAPER_SKIP is deprecated (API start=timestamp, not offset). Omit or leave 0; value is ignored.

# LLM provider: "openai" (default) or "openrouter"
LLM_PROVIDER=openai
# Model used for analysis + comparison (and agent in Phase 2)
LLM_MODEL=gpt-4o-mini
LLM_AGENT_MODEL=gpt-4o-mini

# OpenAI — leave blank to use mock analysis
OPENAI_API_KEY=

# OpenRouter — alternative LLM provider (set LLM_PROVIDER=openrouter)
OPENROUTER_API_KEY=

# Google Places — leave blank to extract name from Maps URL
GOOGLE_PLACES_API_KEY=

# JWT auth (use 32+ characters to avoid HMAC length warnings)
JWT_SECRET_KEY=change-me-in-production-min-32-chars
JWT_EXPIRE_MINUTES=1440

# Extra CORS origins for remote staging (comma-separated). Localhost is always allowed.
# Example: https://my-frontend.up.railway.app
CORS_ORIGINS=

# MongoDB (optional) — polyglot persistence for analysis history, comparison cache, raw responses.
# Leave empty to disable all MongoDB features (app works exactly as before).
MONGO_URI=
# MONGO_URI=mongodb://mongo:27017   (Docker Compose)
# MONGO_URI=mongodb://localhost:27017 (local dev)
MONGO_DB_NAME=review_insight
COMPARISON_CACHE_TTL_HOURS=24
RAW_RESPONSE_TTL_DAYS=30
