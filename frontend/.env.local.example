NEXT_PUBLIC_API_URL=http://localhost:8000

# Set to "true" to enable the debug event trail (developer only; off by default)
# Shows a floating panel with recent events: API calls, route changes, user actions, errors.
# NEXT_PUBLIC_DEBUG_TRAIL=true
